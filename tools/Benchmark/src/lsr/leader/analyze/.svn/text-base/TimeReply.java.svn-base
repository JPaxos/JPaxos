package lsr.leader.analyze;


public class TimeReply extends LogRecord {

	public TimeReply(String logLine, int procID, String[] tokens, long timestamp) {
		super(LogType.TimeReply, logLine, procID, timestamp);
		
//		view = Integer.parseInt(tokens[4]);
	}

}
