package benchmark;

import process_handlers.ProcessHandler;

public interface ProcessListener {

	void errorCaught(ProcessHandler processHandler);

	void processCreated(ProcessHandler processHandler);

	void processFinished(ProcessHandler processHandler);

	void clientSent(ProcessHandler processHandler);

}
