# Specify the handlers to create in the root logger
# (all loggers are children of the root logger)
# The following creates two handlers
handlers = java.util.logging.ConsoleHandler
#handlers = java.util.logging.FileHandler

# Set the default logging level for the root logger
#.level = FINE
.level = INFO
#.level = WARNING
#lsr.paxos.Replica.level=INFO
#lsr.paxos.FailureDetector.level=INFO
#lsr.paxos.Paxos.level=INFO
#lsr.paxos.network.Network.level=FINEST

# Set the default logging level for new ConsoleHandler instances
java.util.logging.ConsoleHandler.level = FINE
java.util.logging.FileHandler.level = FINE

# Set the default formatter for new ConsoleHandler instances
java.util.logging.ConsoleHandler.formatter = lsr.common.logFormatters.Benchmark2LogFormatter
java.util.logging.FileHandler.formatter = lsr.common.logFormatters.Benchmark2LogFormatter
java.util.logging.FileHandler.pattern=servers.log

